# Style oracle for humans and AI agents alike: `bundle exec standardrb`
# (enforced by the CI lint job). Autofix with `bundle exec standardrb --fix`.
ruby_version: 3.0 # match required_ruby_version in the gemspec
